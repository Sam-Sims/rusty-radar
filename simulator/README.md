# simulator

Simulation display for local testing.

```
cargo watch -w simulator/src -w simulator/Cargo.toml -w graphics/src -w graphics/Cargo.toml -x 'run --target x86_64-unknown-linux-gnu -p rusty-radar-simulator'
```
